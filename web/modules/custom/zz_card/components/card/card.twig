<!-- Horoscope Card -->
<div class="card">
  <div class="zodiac-icon {{ sign }}">{{ icon }}</div> <!-- Example Zodiac Icon -->
  <div class="zodiac-name">{{ title|trans}}</div>
  <div class="horoscope-text">{{ content|raw }}</div>
</div>
